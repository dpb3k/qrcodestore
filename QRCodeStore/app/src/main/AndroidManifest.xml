<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Camera Permissions !-->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:targetApi="31">

        <!-- Declaration of SplashActivity as the launcher -->
        <activity
            android:name=".SplashActivity"
            android:theme="@style/AppTheme"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Declaration of MainActivity -->
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme"
            android:exported="true" />

        <!-- Declaration of ScanBarcodeActivity -->
        <activity
            android:name=".ScanBarcodeActivity"
            android:theme="@style/AppTheme"
            android:exported="false" />

        <!-- Declaration of ProductCartListActivity -->
        <activity
            android:name=".ProductCartListActivity"
            android:theme="@style/AppTheme"
            android:exported="false" />

        <!-- Declaration of UserHomeActivity -->
        <activity
            android:name=".UserHomeActivity"
            android:theme="@style/AppTheme"
            android:exported="false" />

        <!-- Declaration of RegistrationActivity -->
        <activity
            android:name=".RegistrationActivity"
            android:theme="@style/AppTheme"
            android:exported="false" />

        <!-- Declaration of LoginActivity -->
        <activity
            android:name=".LoginActivity"
            android:theme="@style/AppTheme"
            android:windowSoftInputMode="stateHidden"
            android:exported="false" />

        <!-- Declaration of ProductViewActivity -->
        <activity
            android:name=".ProductViewActivity"
            android:theme="@style/AppTheme"
            android:exported="false" />

        <!-- Declaration of ForgotPasswordActivity -->
        <activity
            android:name=".ForgotPasswordActivity"
            android:theme="@style/AppTheme"
            android:exported="false" />

        <!-- Declaration of Bill View Activity -->
        <activity
            android:name=".BillViewActivity"
            android:theme="@style/AppTheme"
            android:exported="false" />

    </application>

</manifest>
